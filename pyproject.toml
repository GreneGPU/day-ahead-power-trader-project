[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "intraday-power-quant"
version = "0.1.0"
description = "Day-Ahead Power Trading Project: transfer-learning 15-minute power-price forecasting and flexibility simulation."
requires-python = ">=3.10"
dependencies = [
  "pandas>=2.2",
  "numpy>=1.26",
  "fastapi>=0.117",
]

[project.optional-dependencies]
training = [
  "openpyxl>=3.1",
  "scikit-learn>=1.5",
  "xgboost>=2.1",
  "lightgbm>=4.5",
  "catboost>=1.2",
  "matplotlib>=3.9",
]
dashboard = ["streamlit>=1.37", "plotly>=5.23"]
mlops = ["mlflow>=3.0,<4", "cloudpickle>=3.0"]
aws = ["boto3>=1.35", "sagemaker>=2.232,<3"]
dev = ["pytest>=8.0", "ruff>=0.6"]

[project.scripts]
intraday-power-quant = "intraday_power_quant.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
